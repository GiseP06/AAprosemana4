let utiles = ["Cartuchera", "Tijera", "Carpeta", "Goma"]

for (let i=0; i<utiles.length; i++ ) {
    console.log (utiles[i])
}

utiles.push ("Lapicera")

for (let i=0; i<utiles.length; i++ ) {
    console.log (utiles[i])
}